# Mudir Autopilot (publish-only)
Posts one pre-made image to Instagram (Instagram Login API) every Tue/Thu/Sat, in the cloud.
Images live in `posts/` with `posts/captions.json`. The Action just publishes the next unposted one.
Secrets required: IG_USER_ID, IG_ACCESS_TOKEN, RAW_BASE_URL.
